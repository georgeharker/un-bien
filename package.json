{
  "name": "@geohar/un-bien-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "un-bien monorepo: native iOS/macOS client (app/), pi extension (extension/), and relay (relay/). Derived from remote-pi (Jacob Moura) — see extension/relay for origin history + license.",
  "main": "extension/dist/index.js",
  "pi": {
    "extensions": [
      "./extension/dist"
    ]
  },
  "scripts": {
    "build": "pnpm -C extension build && pnpm -C launcher build",
    "typecheck": "pnpm -C extension typecheck && pnpm -C launcher typecheck",
    "test": "pnpm -C extension test",
    "format": "prettier --write \"extension/src/**/*.ts\" \"launcher/src/**/*.ts\"",
    "format:check": "prettier --check \"extension/src/**/*.ts\" \"launcher/src/**/*.ts\""
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@11.13.1",
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
